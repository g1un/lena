mixin social()
	+namespace("social")
		+el()
			+el("list", "ul")
				each val in ["vk", "fb", "twitter", "google", "ok"]
					+el("item", "li")
						+el("link", "a")(href="#", target="_blank", class="social-link __#{val}")